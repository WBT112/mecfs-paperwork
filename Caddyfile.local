# Local/CI HTTP-only reverse proxy (no TLS). Use with:
#   docker compose -f compose.prod.yaml -f compose.local-proxy.yaml up -d --build
# or just ignore if you use compose.yaml for local dev.
{
  auto_https off
}

:8080 {
  reverse_proxy web:8080
}
