name: E2E (Playwright)

on:
  pull_request:
    paths:
      - "app/**"
      - ".github/workflows/e2e.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    strategy:
      fail-fast: false
      matrix:
        include:
          - browser: chromium
            allow_failure: false
            repeat_each: 1
          - browser: firefox
            allow_failure: true
            repeat_each: 1
          - browser: webkit
            allow_failure: true
            repeat_each: 1

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Install dependencies
        working-directory: app
        run: npm ci --prefer-offline --no-audit --fund=false

      - name: Install Playwright browsers
        working-directory: app
        run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run Playwright tests (${{ matrix.browser }})
        working-directory: app
        env:
          CI: true
          VITE_ENABLE_DEV_SW: "true"
        run: |
          set +e
          npx playwright test --project=${{ matrix.browser }} --repeat-each ${{ matrix.repeat_each }} --retries=1
          status=$?
          if [ $status -ne 0 ] && [ "${{ matrix.allow_failure }}" = "true" ]; then
            echo "::warning title=E2E failed but allowed::Playwright project '${{ matrix.browser }}' failed. This does not fail the workflow. See Playwright report artifacts for details."
            exit 0
          fi
          exit $status

      - name: Upload Playwright report
        if: failure() || matrix.browser == 'chromium'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-report-${{ matrix.browser }}
          path: |
            app/playwright-report
            app/test-results
          retention-days: 7
