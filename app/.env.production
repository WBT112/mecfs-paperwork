# Production environment configuration
# This file is loaded by Vite when building with --mode production

# Do not show dev-only formpacks in production
# VITE_SHOW_DEV_FORMPACKS is intentionally not set

# Deployment environment marker
VITE_DEPLOYMENT_ENV=production

# Absolute public origin for social previews (Open Graph/Twitter)
VITE_PUBLIC_ORIGIN=https://mecfs-paperwork.de
