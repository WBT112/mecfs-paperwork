import {
  AccessibleSelectWidget,
  AttachmentsAssistantWidget,
  AutoGrowTextareaWidget,
} from './rjsfWidgets';

export const formpackWidgets = {
  autoGrowTextarea: AutoGrowTextareaWidget,
  attachmentsAssistant: AttachmentsAssistantWidget,
  SelectWidget: AccessibleSelectWidget,
  select: AccessibleSelectWidget,
};
